project(
    'apk-toolsd',
    'd',
    meson_version: '>=0.54',
    version: '2.10.5.4',
)

project_soversion = 0

openssld_dep = dependency('openssld')

src_inc = include_directories('deimos/..')

subdir('deimos/apk_toolsd')
